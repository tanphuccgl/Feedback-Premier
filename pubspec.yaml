name: review_premier_pearl
description: ReviewOffline/CreateReviewOffline

publish_to: "none" # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter


  dio: ^4.0.0
  material_floating_search_bar: ^0.3.4
  provider: ^6.0.1
  http_parser: ^4.0.0
  image_picker: ^0.8.4+2
  permission_handler: ^6.0.0
  intl: ^0.17.0
  hexcolor: ^2.0.5
  flutter_spinkit: ^5.1.0
  flutter_svg: ^0.22.0

  carousel_slider: ^4.0.0
  # Service locator
  get_it: ^7.2.0
  # Bloc for state management
  flutter_bloc: ^7.3.0
  flutter_rating_bar: ^4.0.0
  # Value equality
  equatable: ^2.0.3
  # Functional programming thingies
  dartz: ^0.10.0
  # Remote API
  data_connection_checker: ^0.3.0
  http: ^0.13.3
  # Local cache
  shared_preferences: ^2.0.8
  file_picker: ^4.2.2+1
  percent_indicator: ^3.4.0
  date_time_picker: ^2.1.0
  open_file:
  file_icon: ^1.0.0
  image_gallery_saver: ^1.7.1
  painter: ^2.0.0
  flutter_colorpicker: ^1.0.2
  screenshot: ^1.2.3
  path_provider: ^2.0.7
  file_utils: ^0.1.3
  photo_view: ^0.13.0
  uri_to_file: ^0.1.3
  cupertino_icons: ^1.0.2
  fluttertoast: ^8.0.8
  pin_code_fields: ^7.3.0
  awesome_dialog: ^2.1.2
  auto_size_text: ^3.0.0
  readmore: ^2.1.0
  flutter_localizations: 
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0


flutter:

  uses-material-design: true
  generate: true

  assets:
    - assets/
    - assets/icons/
